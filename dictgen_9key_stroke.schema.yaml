# Rime schema settings
# encoding: utf-8

schema:
  schema_id: dictgen_9key_stroke
  name: "（9鍵反查用）五筆畫"
  version: "1.0.1"
  author:
    - 基於 stroke (https://github.com/rime/rime-stroke)

engine:
  processors:
    - speller
  translators:
    - table_translator

speller:
  alphabet: "45678"
  delimiter: " 1"
  algebra:
    - xlit hspnz 45678

translator:
  dictionary: stroke
  prism: dictgen_9key_stroke
